// import { Server, Socket } from "socket.io";

// const io = new Server(3001);

// io.on("connection", (socket) => {
//     socket.emit("connect", "you are connected");
//     socket.on("msg", (data) => {
//         console.log("notif from client", data);
//     });
// });